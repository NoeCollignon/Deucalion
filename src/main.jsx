import React from "react";
import ReactDOM from "react-dom/client";
import PreConsultIA from "./PreConsultIA.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PreConsultIA />
  </React.StrictMode>
);
